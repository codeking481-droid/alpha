import React from 'react';
import { FindCompanies } from './FindCompanies';

// My Ad Campaigns is now the Search Companies engine — its job is to search for companies
// We search for companies from here (Apollo + Hunter verified, Global USA/UK/Global)
export const MyAdCampaigns = () => {
  return <FindCompanies />;
};
