name = "alpha-agency-api"
main = "src/index.js"
compatibility_date = "2024-12-18"
compatibility_flags = ["nodejs_compat"]
workers_dev = true

[triggers]
crons = ["*/2 * * * *"]

[vars]
ENV = "production"
GROQ_MODEL = "openai/gpt-oss-120b"
# This Paystack merchant does not have USD enabled. Prices are entered in USD
# equivalents ($50 Lifetime public / $250 private Founding was $500) and converted to NGN by payment.js before checkout.
PAYSTACK_CURRENCY = "NGN"
PAYSTACK_NGN_RATE = "1500"
# Real Audience We Own - exact numbers (private offer)
TOTAL_REACH = "4671"
AUDIENCE_WHATSAPP = "131"
AUDIENCE_CYBER = "86"
AUDIENCE_TELEGRAM = "184"
AUDIENCE_LINKEDIN = "1270"
AUDIENCE_YOUTUBE = "3000"
# Pricing - Public $50 Lifetime Tool + Private $250 Service (hidden from landing)
PRICE = "50"
PRICE_TOOL = "50"
PRICE_TOOL_CENTS = "5000"
PRICE_SERVICE = "250"
PRICE_SERVICE_CENTS = "25000"
ORIGINAL_PRICE = "500"
ORIGINAL_SERVICE = "500"
STRIPE_AMOUNT = "5000"
STRIPE_TOOL = "5000"
STRIPE_SERVICE = "25000"
PRICE_TOOL_LABEL = "$50 Lifetime"

# Supabase - create project at https://supabase.com then set:
# npx wrangler secret put SUPABASE_URL
# npx wrangler secret put SUPABASE_ANON_KEY
# Optional service key for writes: SUPABASE_SERVICE_KEY
# See supabase.sql for tables (backend/supabase.sql)

# Groq - https://console.groq.com/keys
# npx wrangler secret put GROQ_API_KEY
# npx wrangler secret put GROQ_MODEL  # e.g., openai/gpt-oss-120b, llama3-70b-8192, or your 120b own id (user 120B)
# npx wrangler secret put ACCESS_CODES  # comma-separated, e.g. ALPHA2024,TEAM123

# Telegram hot lead alerts
# npx wrangler secret put TELEGRAM_BOT_TOKEN  # from @BotFather
# npx wrangler secret put TELEGRAM_CHAT_ID    # your chat ID

# Paystack - $50 Lifetime / $250 Private / $500 Regular converted to NGN - https://dashboard.paystack.com/#/settings/developer
# npx wrangler secret put PAYSTACK_SECRET_KEY   # sk_test_... or sk_live_...
# Optional: npx wrangler secret put FRONTEND_URL        # e.g. https://alphatekx.name.ng
